# SOPS configuration
# Age public keys for each host — add your machine's public key here
# Generate with: age-keygen | tee ~/.config/sops/age/keys.txt | age-keygen -y
creation_rules:
  - path_regex: secrets/.*\.yaml$
    age: >-
      age1REPLACE_WITH_YOUR_PUBLIC_KEY
